<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/_3sdp"/>
    <gradient
        android:angle="360"
        android:centerColor="@color/colorBlueLight"
        android:endColor="@color/colorBlueLight"
        android:startColor="@color/colorBlueLight"
        android:type="linear" />
</shape>