<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="2dp"/>
    <gradient
        android:angle="360"
        android:centerColor="@color/colorLightPurpalvery"
        android:endColor="@color/colorLightPurpalvery"
        android:startColor="@color/colorLightPurpalvery"
        android:type="linear" />
</shape>